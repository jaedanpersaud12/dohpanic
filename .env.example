# --- Admin access -----------------------------------------------------------
ADMIN_PASSWORD=changeme

# --- Signing secret (rotate this = every existing QR code stops validating) --
TICKET_SECRET=dev-secret-change-me-to-a-long-random-string

# --- Public base URL (used inside QR codes + WhatsApp links) -----------------
NEXT_PUBLIC_BASE_URL=http://localhost:3000
